name: responsive_wrap
description: "A responsive Wrap widget for Flutter that automatically adjusts item layout based on available width."
version: 1.0.0
homepage: https://flutkit.com
repository: https://github.com/flutkitcom/responsive_wrap
issue_tracker: https://github.com/flutkitcom/responsive_wrap/issues

topics:
  - responsive
  - layout
  - wrap
  - dashboard
  - flutter-web

environment:
  sdk: ^3.12.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0


